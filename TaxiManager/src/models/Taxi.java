package models;import java.util.regex.Pattern;public class Taxi {    /** registration number of the taxi **/    private String RegNum;    /** Number of seat in the taxi **/    private int SeatsNum;    /** Regex **/    private static final String REGEX_REG_NB = "[A-Z]{3}_[A-Z0-9]{6}";    /** Errors **/    private static final String ERROR_NULL_ARGUEMENT = "Driver name and " +            "registration number shold not be null";    /** Constructor    * driverName name of the driver of the taxi    * regNb registration number or the taxi    * RegNbFormatException    * */    public Taxi(String RegNum, int SeatsNum){    // Check arguements        if( SeatsNum == 0 || RegNum == null){            throw new NullPointerException(ERROR_NULL_ARGUEMENT);        }    // Check conformity of the registration number        Pattern regex = Pattern.compile(REGEX_REG_NB);        Matcher match = regex.matcher(RegNum);        if(RegNum.length() != 10 || !match.find()){            throw new RegNumFormatException(RegNum);        }    // Initiate variables       this.RegNum = RegNum;        this.SeatsNum = SeatsNum;    }    /* Get the registration number of taxi */    public String getRegNum() {        return RegNum;    }    public void setRegNum(String regNum) {        RegNum = regNum;    }    public int getSeatsNum() {        return SeatsNum;    }    public void setSeatsNum(int seatsNum) {        SeatsNum = seatsNum;    }    /* Equal method*/    public boolean equals(Object other){        boolean eq = false;        if(other != null && other instanceof Taxi){            Taxi t = (Taxi) other;            eq = this.getRegNum().equals(t.getRegNum());        }        return eq;    }    /* CompareTo method */    public int compareTo(Taxi other){        return RegNum.compareTo(other.RegNum);    }    /* ToString method */    public String toString(){        return RegNum + ", Nb seats: " + SeatsNum;    }}